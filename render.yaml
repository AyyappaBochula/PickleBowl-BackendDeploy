services:
  - type: web
    name: picklebowl
    runtime: python
    plan: free
    rootDir: Backend
    buildCommand: npm --prefix ../Frontend ci && npm --prefix ../Frontend run build && pip install -r requirements.txt && python manage.py collectstatic --noinput --clear && python manage.py migrate && python manage.py seed_admins
    startCommand: gunicorn Backend.wsgi:application
    healthCheckPath: /api/products/
    envVars:
      - key: DJANGO_SECRET_KEY
        generateValue: true
      - key: DEBUG
        value: "false"
      - key: ALLOWED_HOSTS
        value: .onrender.com
      - key: DATABASE_URL
        sync: false
      - key: RAZORPAY_KEY_ID
        sync: false
      - key: RAZORPAY_KEY_SECRET
        sync: false
      - key: DJANGO_SUPERUSER_USERNAME
        sync: false
      - key: DJANGO_SUPERUSER_EMAIL
        sync: false
      - key: DJANGO_SUPERUSER_PASSWORD
        sync: false
      - key: ADMIN_NAME
        sync: false
      - key: ADMIN_MOBILE
        sync: false
      - key: ADMIN_PASSWORD
        sync: false
